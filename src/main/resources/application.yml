server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://172.16.26.168:3306/robot_data_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: rootuser
    password: Zyy060116
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

scheduler:
  priority:
    weight:
      base: 1.0
      waiting: 1.0
      deadline: 1.0
      type: 1.0
      robot: 1.0
    recalculation-interval-ms: 30000

llm:
  websocket:
    url: ws://localhost:8090/ws/llm
    timeout-ms: 5000

rosbridge:
  http:
    url: http://172.16.25.219:9090
  default-robot-code: ""

# 运动学算法对接配置
motion:
  websocket:
    url: ws://localhost:8081/ws/motion
    timeout-ms: 10000

# 数据服务对接配置
data-service:
  url: http://localhost:8000
  connect-timeout-ms: 3000
  read-timeout-ms: 5000
  retry:
    max-attempts: 3
    backoff-multiplier: 1000
